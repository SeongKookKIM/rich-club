import React from "react";
import { Section } from "react-fullpage";

function Tip() {
  return (
    <Section className="tip">
      <div className="tip-inner">Tip</div>
    </Section>
  );
}

export default Tip;
